#include <cstdio>

#define MAX 10010
int x[MAX];
int y[MAX];
int main() {
	int M, N;

	while (scanf("%d%d", &M, &N) == 2) {
		int i;
		for (i = 0; i < M; ++i) {
			scanf("%d%d", &x[i], &y[i]);
		}

	}
	return 0;
}